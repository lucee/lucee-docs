```luceescript+trycf
	  world = {
        save:"water",
        plant:"trees",
        clean:"waste"
    };
    writedump(world);
    updateStruct = structupdate(world,"clean","room");
    writeDump(updatestruct);
```