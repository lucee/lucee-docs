```luceescript+trycf
	datetime = createdate(1997,04,11);
	writeOutput("Total number of days for the month is " & datetime.daysInMonth());
	date = now();
	writeOutput("Total number of days for the current month is " & date.daysInMonth());
```
