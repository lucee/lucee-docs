```luceescript+trycf
writeDump(label="getTickCount (milli/default)",
    var=getTickCount() );
writeDump(label="nano",
    var=getTickCount('nano') );
writeDump(label="second",
    var=getTickCount('second') );
```
