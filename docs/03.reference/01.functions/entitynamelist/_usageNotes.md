Returns the names of all persistent entities mapped in the current application as a string list. Pass a custom delimiter as the argument — defaults to comma.

Useful for debugging or building dynamic admin interfaces. See also [[function-entitynamearray]].
