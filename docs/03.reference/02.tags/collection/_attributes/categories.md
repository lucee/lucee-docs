Boolean. Enables category support for the collection.
Used only when `action="create"`.
When enabled, documents indexed with `cfindex` can be assigned categories using the `category` and `categoryTree` attributes, and searches can be filtered by category.